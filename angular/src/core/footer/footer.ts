import {Breadcrumb} from "../breadcrumbs/breadcrumb";

export class Footer {
    breadcrumbs: Breadcrumb[];
    editFlag: boolean;
    refreshFlag: boolean;
    sourceFlag: boolean;
    updated: string;
}
